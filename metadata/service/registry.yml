parameters:
  docker-composition:
    registry:
      enabled: true
      compose:
        version: '3.1'
        services:
          registry:
            hostname: registry
            image: registry:2.6.2
