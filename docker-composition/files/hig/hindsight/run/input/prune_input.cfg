filename = "prune_input.lua"
ticker_interval = {{ cfg.get('ticker_interval', 61) }}

-- Override of default sandbox configuration variables
{%- for key in ['output_limit', 'memory_limit', 'instruction_limit', 'preserve_data', 'restricted_headers', 'shutdown_on_terminate'] %}
{%- if key in cfg %}
{{ key }} = {{ cfg[key] }}
{%- endif %}
{%- endfor %}

input = {{ cfg.get('input', True)|lower }}
analysis = {{ cfg.get('analysis', True)|lower }}
